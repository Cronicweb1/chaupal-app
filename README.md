# Chaupal App

Flutter board-games and baithak app.

## APK preview

GitHub Actions builds `chaupal-preview.apk` on every push to `main` and can also be started manually from the Actions tab. Download the generated APK from the workflow run's Artifacts section.

## Source

The Flutter project lives under `app/`.
